import myData from "./dataset.json" assert { type: "json" };

datas.forEach((data) => {
  const div = document.createElement("div");
  div.setAttribute("class", "box");
  div.textContent = data;
  document.getElementById("bs__wrapper3").append(div);
});
